VITE_FELLOW_API_KEY=replace-with-actual-key-or-build-arg
VITE_API_BASE=/api
# When mounted under a path prefix in nginx, set both:
#   VITE_BASE_PATH=/coffee/      (Vite asset URL prefix; trailing slash required)
#   VITE_API_BASE=/coffee/api    (where the SPA sends API calls)
VITE_BASE_PATH=/
